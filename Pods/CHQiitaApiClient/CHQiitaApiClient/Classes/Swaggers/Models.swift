// Models.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation

protocol JSONEncodable {
    func encodeToJSON() -> Any
}

public enum ErrorResponse : Error {
    case Error(Int, Data?, Error)
}

open class Response<T> {
    open let statusCode: Int
    open let header: [String: String]
    open let body: T?

    public init(statusCode: Int, header: [String: String], body: T?) {
        self.statusCode = statusCode
        self.header = header
        self.body = body
    }

    public convenience init(response: HTTPURLResponse, body: T?) {
        let rawHeader = response.allHeaderFields
        var header = [String:String]()
        for (key, value) in rawHeader {
            header[key as! String] = value as? String
        }
        self.init(statusCode: response.statusCode, header: header, body: body)
    }
}

private var once = Int()
class Decoders {
    static fileprivate var decoders = Dictionary<String, ((AnyObject, AnyObject?) -> AnyObject)>()

    static func addDecoder<T>(clazz: T.Type, decoder: @escaping ((AnyObject, AnyObject?) -> T)) {
        let key = "\(T.self)"
        decoders[key] = { decoder($0, $1) as AnyObject }
    }

    static func decode<T>(clazz: T.Type, discriminator: String, source: AnyObject) -> T {
        let key = discriminator;
        if let decoder = decoders[key] {
            return decoder(source, nil) as! T
        } else {
            fatalError("Source \(source) is not convertible to type \(clazz): Maybe swagger file is insufficient")
        }
    }

    static func decode<T>(clazz: [T].Type, source: AnyObject) -> [T] {
        let array = source as! [AnyObject]
        return array.map { Decoders.decode(clazz: T.self, source: $0, instance: nil) }
    }

    static func decode<T, Key: Hashable>(clazz: [Key:T].Type, source: AnyObject) -> [Key:T] {
        let sourceDictionary = source as! [Key: AnyObject]
        var dictionary = [Key:T]()
        for (key, value) in sourceDictionary {
            dictionary[key] = Decoders.decode(clazz: T.self, source: value, instance: nil)
        }
        return dictionary
    }

    static func decode<T>(clazz: T.Type, source: AnyObject, instance: AnyObject?) -> T {
        initialize()
        if T.self is Int32.Type && source is NSNumber {
            return (source as! NSNumber).int32Value as! T
        }
        if T.self is Int64.Type && source is NSNumber {
            return (source as! NSNumber).int64Value as! T
        }
        if T.self is UUID.Type && source is String {
            return UUID(uuidString: source as! String) as! T
        }
        if source is T {
            return source as! T
        }
        if T.self is Data.Type && source is String {
            return Data(base64Encoded: source as! String) as! T
        }

        let key = "\(T.self)"
        if let decoder = decoders[key] {
           return decoder(source, instance) as! T
        } else {
            fatalError("Source \(source) is not convertible to type \(clazz): Maybe swagger file is insufficient")
        }
    }

    static func decodeOptional<T>(clazz: T.Type, source: AnyObject?) -> T? {
        if source is NSNull {
            return nil
        }
        return source.map { (source: AnyObject) -> T in
            Decoders.decode(clazz: clazz, source: source, instance: nil)
        }
    }

    static func decodeOptional<T>(clazz: [T].Type, source: AnyObject?) -> [T]? {
        if source is NSNull {
            return nil
        }
        return source.map { (someSource: AnyObject) -> [T] in
            Decoders.decode(clazz: clazz, source: someSource)
        }
    }

    static func decodeOptional<T, Key: Hashable>(clazz: [Key:T].Type, source: AnyObject?) -> [Key:T]? {
        if source is NSNull {
            return nil
        }
        return source.map { (someSource: AnyObject) -> [Key:T] in
            Decoders.decode(clazz: clazz, source: someSource)
        }
    }

    private static var __once: () = {
        let formatters = [
            "yyyy-MM-dd",
            "yyyy-MM-dd'T'HH:mm:ssZZZZZ",
            "yyyy-MM-dd'T'HH:mm:ss.SSSZZZZZ",
            "yyyy-MM-dd'T'HH:mm:ss'Z'",
            "yyyy-MM-dd'T'HH:mm:ss.SSS",
            "yyyy-MM-dd HH:mm:ss"
        ].map { (format: String) -> DateFormatter in
            let formatter = DateFormatter()
            formatter.locale = Locale(identifier: "en_US_POSIX")
            formatter.dateFormat = format
            return formatter
        }
        // Decoder for Date
        Decoders.addDecoder(clazz: Date.self) { (source: AnyObject, instance: AnyObject?) -> Date in
           if let sourceString = source as? String {
                for formatter in formatters {
                    if let date = formatter.date(from: sourceString) {
                        return date
                    }
                }
            }
            if let sourceInt = source as? Int64 {
                // treat as a java date
                return Date(timeIntervalSince1970: Double(sourceInt / 1000) )
            }
            fatalError("formatter failed to parse \(source)")
        } 

        // Decoder for [Comment]
        Decoders.addDecoder(clazz: [Comment].self) { (source: AnyObject, instance: AnyObject?) -> [Comment] in
            return Decoders.decode(clazz: [Comment].self, source: source)
        }
        // Decoder for Comment
        Decoders.addDecoder(clazz: Comment.self) { (source: AnyObject, instance: AnyObject?) -> Comment in
            let sourceDictionary = source as! [AnyHashable: Any]
            let result = Comment(id: Decoders.decode(clazz: String.self, source: sourceDictionary["id"]! as AnyObject, instance: nil))
            
            result.user = Decoders.decodeOptional(clazz: User.self, source: sourceDictionary["user"] as AnyObject?)

            result.body = Decoders.decodeOptional(clazz: String.self, source: sourceDictionary["body"] as AnyObject?)

            result.renderedBody = Decoders.decodeOptional(clazz: String.self, source: sourceDictionary["rendered_body"] as AnyObject?)

            result.createdAt = Decoders.decodeOptional(clazz: Date.self, source: sourceDictionary["created_at"] as AnyObject?)

            result.updatedAt = Decoders.decodeOptional(clazz: Date.self, source: sourceDictionary["updated_at"] as AnyObject?)
            return result
        }


        // Decoder for [Item]
        Decoders.addDecoder(clazz: [Item].self) { (source: AnyObject, instance: AnyObject?) -> [Item] in
            return Decoders.decode(clazz: [Item].self, source: source)
        }
        // Decoder for Item
        Decoders.addDecoder(clazz: Item.self) { (source: AnyObject, instance: AnyObject?) -> Item in
            let sourceDictionary = source as! [AnyHashable: Any]
            let result = Item(id: Decoders.decode(clazz: String.self, source: sourceDictionary["id"]! as AnyObject, instance: nil), title: Decoders.decode(clazz: String.self, source: sourceDictionary["title"]! as AnyObject, instance: nil))
            
            result.user = Decoders.decodeOptional(clazz: User.self, source: sourceDictionary["user"] as AnyObject?)

            result.body = Decoders.decodeOptional(clazz: String.self, source: sourceDictionary["body"] as AnyObject?)

            result.renderedBody = Decoders.decodeOptional(clazz: String.self, source: sourceDictionary["rendered_body"] as AnyObject?)

            result.url = Decoders.decodeOptional(clazz: String.self, source: sourceDictionary["url"] as AnyObject?)

            result.likesCount = Decoders.decodeOptional(clazz: Int32.self, source: sourceDictionary["likes_count"] as AnyObject?)

            result.commentsCount = Decoders.decodeOptional(clazz: Int32.self, source: sourceDictionary["comments_count"] as AnyObject?)

            result.tags = Decoders.decodeOptional(clazz: Array.self, source: sourceDictionary["tags"] as AnyObject?)

            result.createdAt = Decoders.decodeOptional(clazz: Date.self, source: sourceDictionary["created_at"] as AnyObject?)

            result.updatedAt = Decoders.decodeOptional(clazz: Date.self, source: sourceDictionary["updated_at"] as AnyObject?)
            return result
        }


        // Decoder for [Tag]
        Decoders.addDecoder(clazz: [Tag].self) { (source: AnyObject, instance: AnyObject?) -> [Tag] in
            return Decoders.decode(clazz: [Tag].self, source: source)
        }
        // Decoder for Tag
        Decoders.addDecoder(clazz: Tag.self) { (source: AnyObject, instance: AnyObject?) -> Tag in
            let sourceDictionary = source as! [AnyHashable: Any]
            let result = Tag()
            
            result.name = Decoders.decodeOptional(clazz: String.self, source: sourceDictionary["name"] as AnyObject?)
            return result
        }


        // Decoder for [User]
        Decoders.addDecoder(clazz: [User].self) { (source: AnyObject, instance: AnyObject?) -> [User] in
            return Decoders.decode(clazz: [User].self, source: source)
        }
        // Decoder for User
        Decoders.addDecoder(clazz: User.self) { (source: AnyObject, instance: AnyObject?) -> User in
            let sourceDictionary = source as! [AnyHashable: Any]
            let result = User(id: Decoders.decode(clazz: String.self, source: sourceDictionary["id"]! as AnyObject, instance: nil))
            
            result.name = Decoders.decodeOptional(clazz: String.self, source: sourceDictionary["name"] as AnyObject?)

            result.description = Decoders.decodeOptional(clazz: String.self, source: sourceDictionary["description"] as AnyObject?)

            result.followeesCount = Decoders.decodeOptional(clazz: Int32.self, source: sourceDictionary["followees_count"] as AnyObject?)

            result.followersCount = Decoders.decodeOptional(clazz: Int32.self, source: sourceDictionary["followers_count"] as AnyObject?)

            result.itemsCount = Decoders.decodeOptional(clazz: Int32.self, source: sourceDictionary["items_count"] as AnyObject?)

            result.profileImageUrl = Decoders.decodeOptional(clazz: String.self, source: sourceDictionary["profile_image_url"] as AnyObject?)

            result.websiteUrl = Decoders.decodeOptional(clazz: String.self, source: sourceDictionary["website_url"] as AnyObject?)
            return result
        }
    }()

    static fileprivate func initialize() {
        _ = Decoders.__once
    }
}
